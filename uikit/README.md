# TEMPLATE LEGACY — not the live app

This `uikit/` tree is unused PhoenixCoded **SaasAble** UI kit copies
(`react/` and `tailwind/`). It is **not** deployed.

**Live product:** repository root (`src/`, Vite, Vercel →
accounts.jdproductions.io).

Do not import from here. Do not treat these kits as JD Productions
Accounts. Do not delete in a drive-by PR unless a human confirms no
lingering references.

See root [AGENTS.md](../AGENTS.md).
