# TEMPLATE LEGACY — not the live app

This `admin/` tree is unused PhoenixCoded **SaasAble** admin copies
(`nextjs/` and `vite/`). It is **not** deployed.

**Live product:** repository root (`src/`, Vite, Vercel →
accounts.jdproductions.io).

Do not import from here. Do not follow the SaasAble READMEs in these
subfolders as product docs. Do not delete in a drive-by PR unless a human
confirms no lingering references.

See root [AGENTS.md](../AGENTS.md).
